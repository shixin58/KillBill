<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bride.demon">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:name=".DemonApplication"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.RecyclerViewActivity"
            android:label="@string/activity_recycler_view"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>

                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="max"
                    android:host="demon"
                    android:pathPrefix="/recycler.view"/>
            </intent-filter>

        </activity>

        <activity
            android:name=".activity.TestFragmentActivity"
            android:label="@string/activity_test_fragment"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="com.bride.demon.activity.TestFragmentActivity"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <activity android:name=".activity.BlankActivity"
            android:label="@string/activity_blank"
            android:screenOrientation="portrait" >
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <activity android:name=".activity.SecondActivity"
            android:label="@string/activity_second"
            android:screenOrientation="portrait"
            android:launchMode="singleInstance" />

        <activity android:name=".activity.ThirdActivity"
            android:label="@string/activity_third"
            android:screenOrientation="portrait"
            android:launchMode="singleTask"/>

        <activity android:name=".activity.NestedListActivity"
            android:label="@string/activity_nested_list"
            android:screenOrientation="portrait"/>

        <activity android:name=".activity.TestTouchActivity"
            android:label="@string/activity_test_touch"
            android:screenOrientation="portrait"
            android:launchMode="standard">
            <intent-filter>

                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="max"
                    android:host="demon"
                    android:pathPrefix="/test.touch"/>
            </intent-filter>
        </activity>

        <activity android:name=".activity.UploadActivity"
            android:label="@string/activity_upload"
            android:launchMode="standard"
            android:screenOrientation="portrait"/>

        <activity android:name=".activity.WebpActivity"
            android:label="@string/activity_webp"
            android:launchMode="standard"
            android:screenOrientation="portrait"/>

        <activity android:name=".activity.GlideActivity"
            android:label="@string/activity_glide"
            android:launchMode="standard"
            android:screenOrientation="portrait"/>

        <service android:name=".service.MyService"
            android:process=":MyService">
            <intent-filter>
                <action android:name="com.bride.demon.IMyService"/>
            </intent-filter>
        </service>

        <service android:name=".service.UploadService"/>
    </application>

</manifest>