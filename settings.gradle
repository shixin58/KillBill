rootProject.name = 'KillBill'

include ':app', ':ui_lib', ':buildSrc'

include ':client'

include ':thirdparty'

include ':widget'

include ':baselib'
project(':baselib').projectDir = new File(rootDir, 'baselib')